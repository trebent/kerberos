{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://trebent.com/kerberos/schemas/oas_schema.json",
  "type": "object",
  "properties": {
    "mappings": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "properties": {
          "backend": {
            "type": "string",
            "description": "The name of the backend this OAS mapping applies to."
          },
          "specification": {
            "type": "string",
            "description": "File path to the OAS to use for this backend."
          },
          "options": {
            "type": "object",
            "description": "Additional options for the OAS backend.",
            "default": {},
            "properties": {
              "validateBody": {
                "type": "boolean",
                "description": "When true, the request bodies of incoming requests will be validated against the OAS.",
                "default": true
              }
            },
            "additionalProperties": false
          }
        },
        "required": [
          "backend",
          "specification"
        ],
        "additionalProperties": false
      }
    },
    "order": {
      "$ref": "http://trebent.com/kerberos/schemas/ordered_schema.json"
    }
  },
  "required": [
    "mappings"
  ],
  "additionalProperties": false
}